/*var sizeGame = 12;  // default size of the Game
var selectedPlayers = 2; // default number of players
var defaultTiles = 10; // default number of tiles for each player

function numberPlayers() {
  players = document.getElementById("selectedPlayers");
  selectedPlayers = players.options[players.selectedIndex].value;
  return selectedPlayers;
}

function sizeGame() {
  var hands = document.getElementById("sizeGame");
  var numberHands = hands.options[hands.selectedIndex].value;
  return numberHands;
}*/
